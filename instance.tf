# TODO 4: Create a Compute instance with an external IP
# TODO 5: Install a webserver on the instance
# e.g. nginx: sudo apt update && sudo apt install nginx -y
# https://registry.terraform.io/providers/hashicorp/google/latest/docs/resources/compute_instance